poetry.lock генерируется автоматически командой `poetry install`
на основе pyproject.toml. Не редактируйте вручную — закоммитьте
файл, который создастся у Tech Lead при первой установке, и
все остальные участники получат ПОБИТОВО одинаковые версии
зависимостей через `poetry install` без флагов.
